# THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY. RUN scripts/combineSchemas TO REGENERATE

schema {
  query: Query
  mutation: Mutation
}

type ActivatedPowerup @aws_cognito_user_pools {
  name: Powerup
  expiresAt: AWSDateTime
}

type AnyUser implements User @aws_cognito_user_pools {
  userId: ID!
  name: String!
}

enum Avatar {
  AZURE_ENGINEER
  YOUTUBER
  SAFE_DISTANCING_AMBASSADOR
  ARTIST
  AWS_ENGINEER
}

type AvatarItem implements StockItem @aws_cognito_user_pools {
  name: Avatar!
  quantity: Int!
  price: Int!
}

type Camera @aws_cognito_user_pools {
  cameraId: ID!
  name: String!
  latitude: String!
  longitude: String!
  address: String!
}

type CrowdsourceReward @aws_cognito_user_pools {
  pass: Boolean!
  coinsEarned: Int!
  coinTotal: Int!
}

type Defendant implements User @aws_cognito_user_pools {
  userId: ID!
  name: String!
  imageUrl: AWSURL!
  appealId: ID!
  appealTime: AWSDateTime!
}

type Friend implements User @aws_cognito_user_pools {
  userId: ID!
  name: String!
  befriendDate: AWSDateTime!
  pairStreak: Streak!
}

type IncriminatingImage @aws_cognito_user_pools {
  captureTime: AWSDateTime!
  imageUrl: AWSURL!
  camera: Camera!
}

input InputUser {
  userId: ID!
  name: String!
}

input LabelledImage {
  label: Boolean!
  imageUrl: AWSURL!
  appealId: ID!
}

enum LeaderboardDivision {
  JEFFBEZOS
  FORTUNE500
  TITANIUM
  PLATINUM
  GOLD
  SILVER
  BRONZE
}

type LeaderboardRow @aws_cognito_user_pools {
  userId: ID!
  division: LeaderboardDivision!
  rank: Int
  coinTotal: Int!
  currentStreak: Int!
  name: String!
}

type LoadedAppeal @aws_cognito_user_pools {
  defendant: Defendant!
  isTest: Boolean!
}

type MaskOnReward @aws_cognito_user_pools {
  coinsEarned: Int!
  coinTotal: Int!
  currentStreak: Int!
}

type Mutation {
  createBubble(user: InputUser!): ID! @aws_cognito_user_pools
  entangleBubble(bubbleId: ID!, newcomer: InputUser!): Int!
    @aws_cognito_user_pools
  untangleBubble(userId: ID!): Int! @aws_cognito_user_pools
  makeFriend(initiator: InputUser!, newFriend: InputUser!): Friend!
    @aws_cognito_user_pools
  crowdsourceLabelImage(
    jurorUserId: ID!
    labels: [LabelledImage!]!
  ): CrowdsourceReward! @aws_cognito_user_pools
  submitAppeal(user: InputUser!, imageUrl: AWSURL!): ID! @aws_cognito_user_pools
  createProfile(userId: ID!, name: String!, faceIdUrl: AWSURL!): Profile!
    @aws_cognito_user_pools
  earnCoins(userId: ID!, nearCameraId: ID!): MaskOnReward!
    @aws_cognito_user_pools
  identifyCitizen(imageUrl: AWSURL!, cameraId: ID!, judgement: Boolean!): Int!
    @aws_iam
  activatePowerup(userId: ID!, powerup: Powerup!): ActivatedPowerup!
    @aws_cognito_user_pools
  buy(userId: ID!, price: Int!, avatar: Avatar, powerup: Powerup): Int!
    @aws_cognito_user_pools
  selectAvatar(userId: ID!, avatar: Avatar!): Avatar! @aws_cognito_user_pools
}

type Pong @aws_cognito_user_pools {
  pong: String!
}

enum Powerup {
  FREEZE
  X2COIN
  X3COIN
}

type PowerupItem implements StockItem @aws_cognito_user_pools {
  name: Powerup!
  quantity: Int!
  price: Int!
}

type Profile implements User @aws_cognito_user_pools {
  name: String!
  userId: ID!
  leaderboard: LeaderboardRow!
  bubbleId: ID
  friends: [Friend!]!
  arsenal: StockRoom!
  faceIdUrl: AWSURL
}

type Query {
  getBubble(bubbleId: ID!): [AnyUser!]! @aws_cognito_user_pools
  listFriends(userId: ID!): [Friend!]! @aws_cognito_user_pools
  getLeaderboardClosest(userId: ID!, nClosest: Int): [LeaderboardRow!]!
    @aws_cognito_user_pools
  getLeaderboardTop(nTop: Int): [LeaderboardRow!]! @aws_cognito_user_pools
  loadAppeals(imageCount: Int): [LoadedAppeal!]! @aws_cognito_user_pools
  ping(ping: String!): Pong! @aws_cognito_user_pools
  getProfile(userId: ID!, isMe: Boolean!): Profile! @aws_cognito_user_pools
  getCameras: [Camera!]! @aws_cognito_user_pools
  getIncriminatingImage(userId: ID!): IncriminatingImage!
    @aws_cognito_user_pools
  listArsenal(userId: ID!): StockRoom! @aws_cognito_user_pools
}

interface StockItem {
  quantity: Int!
  price: Int!
}

type StockRoom @aws_cognito_user_pools {
  powerups: [PowerupItem!]
  avatars: [AvatarItem!]!
  coinTotal: Int!
  currentAvatar: Avatar!
  currentPowerup: ActivatedPowerup
  purchaseSuccess: Boolean
}

type Streak @aws_cognito_user_pools {
  max: Int!
  current: Int!
  isFrozen: Boolean!
}

interface User {
  userId: ID!
  name: String!
}
