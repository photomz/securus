extend type Mutation {
  # POST
  # Excursion group "leader" or initiator taps "New Bubble Group" button.
  # Adds leader to bubbleId. Leader then taps his phone with friends' phones, which triggers entangleBubbleGroup.

  createBubble(user: InputUser!): ID! @aws_api_key
}
